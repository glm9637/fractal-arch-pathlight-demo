name: state_machine_flutter
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: 'none'


resolution: workspace
environment:
  sdk: ^3.11.4
  flutter: '>=3.0.0'

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
